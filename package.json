{
  "name": "api",
  "version": "1.0.0",
  "description": "api to detect faces and recommed food products based on past orders",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "eslint": "^5.16.0",
    "typeorm": "^0.2.17",
    "typescript": "^3.4.5"
  }
}
